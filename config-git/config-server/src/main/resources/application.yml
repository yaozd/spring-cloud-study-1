spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yaozd/spring-cloud-study-1
          search-paths: 'config-repo,{application}'
server:
  port: 12000